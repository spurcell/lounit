// tests the main module

tests is {

    "exit code is 0 when all passing": <-> (test) {
        
        test.assert(true);
    },
    
    "exit code is 1 when there are failures": <-> (test) {
        
        test.assert(true);
    }
};